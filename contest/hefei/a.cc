#include <iostream>
#include <cstdio>
#include <cstring>
using namespace std;
const int N = 30000 + 1;
int n, t, num[N], n1[N], n2[N], l1[N], l2[N], a[N], b[N], sa[N], sb[N];
int main() {
    while (scanf("%d%d", &n, &t) == 2) {
        for (int i = 0; i < n; ++i) {
            scanf("%d", &num[i]);
        }
    }
    return 0;
}
