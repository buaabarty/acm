#include <iostream>
#include <cstdio>
#include <cstring>
using namespace std;
int main()
{
    int t, n, m, ;
    scanf("%d", &t);
    while (t--)
    {
        scanf("%d%d", &n, &m);
        for (int i = 1; i <= m; ++i)
            for (int j = 1;)
    }
    return 0;
}
