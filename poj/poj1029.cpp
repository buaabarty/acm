#include <iostream>
#include <cstdio>
#include <cstring>
#define N 1024
using namespace std;
int n, k;
bool a[N], b[N];
int main()
{
    while (~scanf("%d%d", &n, &k))
    {
        memset(a, false, sizeof(a));
        memset(b, false, sizeof(b));
        for (int i = 1; )
    }
    return 0;
}
